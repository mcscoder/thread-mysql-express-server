export type DateTimeEntity = {
  create_at: string;
  update_at: string;
};
export type DateTime = {
  createdAt: number;
  updatedAt: number;
};
export type DateTimeProp = {
  dateTime: DateTime;
};
